1.join():
	将数组中所有元素都转化为字符串并连接在一起，返回最后生成的字符串。
	默认使用逗号为分隔符。
2.reverse():
	将数组元素倒序输出。
3.sort():
	排序后输出数组。
	以字母表顺序排序。
4.concat():
	创建并返回新数组。
	其返回的数组是 原数组+新添加元素
5.slice(): 参数: 开始下标，结束下标
	截取字符串中某段数组元素
6.splice(): 参数：（插入或删除的起始位置，删除的元素个数） （删除的起始位置，删除个数，插入数组元素）
		  如果省略了第二个参数，则从起始位置到数组结束位置所有元素将都被删除
	在数组中插入或删除元素  删除的是第一个参数下标后面的元素（不包括其自身） 
	起始位置从1开始
7.在数组尾部插入和删除元素
  pop(): 删除数组最后一个元素，返回删除的值。
  push(): 在数组尾部添加一个或多个元素，返回数组新长度。
	两个方法组合，可用来模拟先进后出的栈。
8.在数组头部插入和删除元素
  unshift(): 添加元素，返回数组新长度。 
  shift(): 删除第一个元素，将其返回。
9.toString(): 将数组元素转换为字符串输出，并且带有逗号分隔符。
  toLocalString():一样。
1.返回找到的第一个元素的索引，如果无则返-1
  indexOf(): 从前往后搜索
  lastIndexOf(): 反向
 传递函数！ 
10.forEach(): 传递函数，且函数的参数为value 
	var arr = [1,2,3,4,5];
	var sum = 0;
	arr.forEach(function(value){ sum += value});
	console.log(sum);
11.map():
	将调用的数组的每个元素传递给指定的函数，返回数组。

 了解即可
12.filter():
13.every():
   some():
14.reduce():
   reduceRight();
