1.什么是对象？
  多个数据的封装体（用来保存多个数据的容器）
  一个对象代表现实生活中的一个事物
2.为什么要用对象？
3.对象的组成：
  <1>属性: 分为两个部分 属性名(字符串类型)：属性值（任意类型）
  <2>方法: 一种特别的属性，属性值是函数称为方法
4.如何访问对象内部数据？
  .属性名
  ['属性名']：<1>属性名包含特殊字符： - 空格      
  	      <2>属性名不确定 
	          var p = {};
		  var propName = 'myAge';
		  var value = 18;
		  //p.propName = value    不能使用
		  p[propName] = value;   //将propName的属性值作为变量名来使用，而.的方法是将变量名作为属性名来使用了。
		  console.log(p[propName]);

